global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "inference-api"
    scrape_interval: 10s
    static_configs:
      - targets: ["inference-api:8000"]
        labels:
          service: "nyc-taxi-inference"
